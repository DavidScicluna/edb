import { ReviewsTabProps } from '../../types';

export type UserReviewProps = {
  mediaItem: ReviewsTabProps['mediaItem'];
  mediaType: ReviewsTabProps['mediaType'];
  isLoading: ReviewsTabProps['isLoading'];
};
