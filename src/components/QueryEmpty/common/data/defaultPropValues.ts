import { Space } from '@davidscicluna/component-library';

import { ColorMode } from '@chakra-ui/react';

import { QueryEmptyColor } from '../../types';

export const color: QueryEmptyColor = 'blue';

export const colorMode: ColorMode = 'light';

export const spacing: Space = 4;

export const padding: Space = 4;
