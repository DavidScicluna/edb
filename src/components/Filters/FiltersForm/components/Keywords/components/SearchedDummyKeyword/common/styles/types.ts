import { Theme, Style } from '@davidscicluna/component-library';

import { ColorMode } from '@chakra-ui/react';

export type SearchedDummyKeywordStyleProps = {
	theme: Theme;
	colorMode: ColorMode;
};

export type SearchedDummyKeywordStyleReturn = {
	keyword: Style;
};
