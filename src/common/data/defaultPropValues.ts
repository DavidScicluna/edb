import { ColorMode } from '@chakra-ui/react';

import { UserThemeColor } from '../../store/slices/Users/types';

export const color: UserThemeColor = 'light_blue';

export const colorMode: ColorMode = 'light';
