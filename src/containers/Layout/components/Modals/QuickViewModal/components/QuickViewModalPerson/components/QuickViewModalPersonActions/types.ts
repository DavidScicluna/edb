import { MouseEvent as ME } from 'react';

import { CommonQuickViewModalPersonProps } from '../../common/types';

export type QuickViewModalPersonActionsMouseEvent = ME<HTMLButtonElement, globalThis.MouseEvent>;

export type QuickViewModalPersonActionsProps = CommonQuickViewModalPersonProps;
