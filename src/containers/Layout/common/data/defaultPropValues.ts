import { LayoutDeviceType, LayoutSpacing } from '../../types';

export const device: LayoutDeviceType = 'mobile';

export const isGuest = true;
export const isAuthenticationRoute = false;

export const spacing: LayoutSpacing = 2;
